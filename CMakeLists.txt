cmake_minimum_required(VERSION 3.18.3)
project(esor)
set(THREADS_PREFER_PTHREAD_FLAG ON)
find_package(Threads REQUIRED)
add_executable(esor main.cpp sorts-type.cpp)
target_link_libraries(esor PRIVATE Threads::Threads)
